pipelineymlgen:
  data:
    source:
      ${ inlinetemplate "variables/common.yml" }
  output: self
---
# Include every variables entry that uses "name" and "value" keys.
- ${ inlinerange "v" .source.variables }:
    ${ inlineif (and (hasKey .v "name") (hasKey .v "value")) }:
      ${ yml .v }
