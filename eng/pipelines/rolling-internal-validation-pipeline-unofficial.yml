# Code generated by pipelineymlgen; DO NOT EDIT.
# Origin file: rolling-internal-validation.gen.yml

# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.

# This pipeline runs rolling validation.
# For go-infra: CodeQL on active branch.
# For internal go-mirror and go-images-versions: CG on active branches.

# official: https://dev.azure.com/dnceng/internal/_build?definitionId=1356
# unofficial: https://dev.azure.com/dnceng/internal/_build?definitionId=1496

parameters:
  - name: _info
    displayName: ℹ️ This pipeline runs rolling validation, like CodeQL, for go-infra,
      internal go-mirror, and internal go-images-versions branches.
    type: string
    values:
      - "\U0001F535  rolling-internal-validation-pipeline-unofficial.yml  \U0001F535
        \U0001F535"
    default: "\U0001F535  rolling-internal-validation-pipeline-unofficial.yml  \U0001F535
      \U0001F535"
  - name: enableCodeQL
    displayName: '[Debug input] Enable CodeQL, ignoring cadence. Use to try modifications
      in dev branches.'
    type: boolean
    default: true
  - name: disableTSA
    displayName: '[Debug input] Disable TSA reporting. Use to try modifications in
      dev branches.'
    type: boolean
    default: true
trigger: none
pr: none
schedules: []
variables:
  - template: variables/pipeline.yml
  - template: variables/codeql.yml
  # The default cadence is ok for official branches. The cadence is per-branch.
  # For a dev build, the cadence would prevent dev iteration.
  # https://eng.ms/docs/cloud-ai-platform/devdiv/one-engineering-system-1es/1es-docs/codeql/codeql-general-faq#how-do-i-check-if-my-project-is-onboarded
  - name: Codeql.Cadence
    value: 0
resources:
  repositories:
    - repository: GoMirror
      type: git
      name: internal/microsoft-go-mirror
    - repository: GoImagesVersions
      type: git
      name: internal/microsoft-go-images-versions
    - repository: 1ESPipelineTemplates
      type: git
      name: 1ESPipelineTemplates/1ESPipelineTemplates
      ref: refs/tags/release
extends:
  template: v1/1ES.Unofficial.PipelineTemplate.yml@1ESPipelineTemplates
  parameters:
    pool:
      name: NetCore1ESPool-Internal
      image: 1es-windows-2022
      os: windows
    sdl:
      codeql:
        enabledOnNonDefaultBranches: ${{ parameters.enableCodeQL }}
        language: go
      suppression:
        suppressionFile: $(Build.SourcesDirectory)/.config/guardian/.gdnsuppress
      tsa:
        enabled: ${{ not(parameters.disableTSA) }}
        config:
          codebaseName: 'Go'
          notificationAliases:
            - 'golangdev@microsoft.com'
          codebaseAdmins:
            - 'redmond\golangdev'
          instanceUrl: 'https://devdiv.visualstudio.com/'
          projectName: 'DEVDIV'
          areaPath: 'DevDiv\GoLang'
          iterationPath: 'DevDiv'
          allTools: true
      sourceRepositoriesToScan:
        include:
          - repository: GoMirror
          - repository: GoImagesVersions
    stages:
      # Don't run CodeQL if this is triggered by a push to one of the internal repositories.
      - ${{ if in(variables['Build.Reason'], 'Schedule', 'Manual') }}:
          - stage: GoInfraCodeQL
            displayName: go-infra CodeQL
            dependsOn: []
            jobs:
              - job: Analyze
                workspace:
                  clean: all
                steps:
                  - checkout: self
                  - task: GoTool@0
                    inputs:
                      version: 1.24.1
                  - script: |
                      go build ./...
                    displayName: go
      - stage: MirrorCG
        displayName: go-mirror CG
        dependsOn: []
        jobs:
          - job: MirrorCG
            workspace:
              clean: all
            variables:
              - name: RepositoryUrl
                value: $[resources.repositories.GoMirror.url]
              - name: RepositoryVersion
                value: $[resources.repositories.GoMirror.version]
              # We check out the repository ourselves, so we need to manually
              # specify the repository information.
              - name: CG_ADO_BUILD_REPOSITORY_URI
                value: $[resources.repositories.GoMirror.url]
              - name: CG_ADO_BUILD_SOURCEBRANCH
                value: $[resources.repositories.GoMirror.ref]
              - name: CG_ADO_BUILD_SOURCEVERSION
                value: $[resources.repositories.GoMirror.version]
            steps:
              - template: /eng/pipelines/steps/checkout-windows-task.yml@self
                parameters:
                  Build.Repository.Uri: $(RepositoryUrl)
                  Build.SourceVersion: $(RepositoryVersion)
      - stage: ImagesVersionsCG
        displayName: go-images-versions CG
        dependsOn: []
        jobs:
          - job: ImagesVersionsCG
            workspace:
              clean: all
            variables:
              - name: RepositoryUrl
                value: $[resources.repositories.GoImagesVersions.url]
              - name: RepositoryVersion
                value: $[resources.repositories.GoImagesVersions.version]
              # We check out the repository ourselves, so we need to manually
              # specify the repository information.
              - name: CG_ADO_BUILD_REPOSITORY_URI
                value: $[resources.repositories.GoImagesVersions.url]
              - name: CG_ADO_BUILD_SOURCEBRANCH
                value: $[resources.repositories.GoImagesVersions.ref]
              - name: CG_ADO_BUILD_SOURCEVERSION
                value: $[resources.repositories.GoImagesVersions.version]
            steps:
              - template: /eng/pipelines/steps/checkout-windows-task.yml@self
                parameters:
                  Build.Repository.Uri: $(RepositoryUrl)
                  Build.SourceVersion: $(RepositoryVersion)
