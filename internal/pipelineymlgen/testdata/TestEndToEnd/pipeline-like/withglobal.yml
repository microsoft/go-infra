# Code generated by pipelineymlgen; DO NOT EDIT.
# Origin file: withglobal.gen.yml

# Test with repository-absolute path inlinetemplate (starts with /)

trigger:
  branches:
    include:
      - main
stages:
  - stage: Deploy
    displayName: "Deploy GlobalService"
    jobs:
      - job: DeployJob
        steps:
          - script: echo "Deploying GlobalService"
            displayName: "Deploy GlobalService"
  - stage: Cleanup
    displayName: "Cleanup for GlobalService"
    jobs:
      - job: CleanupJob
        displayName: "Cleanup GlobalService"
        steps:
          - script: echo "Global template cleaning up GlobalService"
            displayName: "Cleanup GlobalService"
          - script: echo "Remove temporary files"
            displayName: "Remove temps"
