# Code generated by pipelineymlgen; DO NOT EDIT.
# Origin file: withlocal.gen.yml

# Test with local path inlinetemplate (relative to .gen.yml file)

trigger: none
stages:
  - stage: Setup
    jobs:
      - job: SetupJob
        steps:
          - script: echo "Setting up LocalProject"
            displayName: Setup
  - # Shared build stage template (local to parent directory)
    stage: Build
    displayName: "Build LocalProject"
    jobs:
      - job: BuildJob
        displayName: "Building LocalProject"
        steps:
          - script: echo "Local template building LocalProject"
            displayName: "Build LocalProject"
          - script: echo "Compile and test"
            displayName: Compile
